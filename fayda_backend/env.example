# Database Configuration
# PostgreSQL 18 Connection (recommended for production and development)
# Update these values to match your local PostgreSQL 18 setup in pgAdmin 4
# Format: postgresql+psycopg://username:password@host:port/database_name
# Example for local PostgreSQL 18:
DATABASE_URL=postgresql+psycopg://postgres:your_password@localhost:5432/faydaidcheck

# If DATABASE_URL is not set, the app will fall back to SQLite (sqlite:///./fapp.db)

# JWT Configuration
JWT_SECRET_KEY=change-me-to-a-secure-secret-key
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Application Environment
APP_ENV=dev

# Legacy compatibility (will be deprecated)
SECRET_KEY=change-me-to-a-secure-secret-key
ALGORITHM=HS256
